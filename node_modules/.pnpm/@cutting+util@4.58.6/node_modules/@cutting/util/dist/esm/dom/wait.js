export const wait = (selector, breakAt = 10, delay = 100) => {
    return new Promise((resolve, reject) => {
        const wait = (count = 0) => {
            const el = document.querySelector(selector);
            if (!!el) {
                resolve(el);
            }
            else {
                setTimeout(() => {
                    if (count < breakAt) {
                        wait(count + 1);
                    }
                    else {
                        reject(new Error(`no element found for ${selector}`));
                    }
                }, delay);
            }
        };
        wait();
    });
};
//# sourceMappingURL=wait.js.map