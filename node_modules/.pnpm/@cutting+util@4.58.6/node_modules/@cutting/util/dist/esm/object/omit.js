export function omit(obj, ...keys) {
    if (!obj) {
        return {};
    }
    const copy = {};
    const remaining = Object.keys(obj).flatMap((c) => (keys.includes(c) === false ? [c] : []));
    for (const k of remaining) {
        copy[k] = obj[k];
    }
    return copy;
}
//# sourceMappingURL=omit.js.map