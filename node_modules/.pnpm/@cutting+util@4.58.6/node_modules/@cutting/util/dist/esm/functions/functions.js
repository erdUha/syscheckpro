import { isDate } from '../date/date.js';
import { isNil } from '../object/isNil.js';
// eslint-disable-next-line @typescript-eslint/no-empty-function
export const noop = () => { };
export const isPromise = (obj) => {
    return (typeof !isNil(obj) && typeof obj === 'object' && obj !== null && 'then' in obj && 'function' === typeof obj['then']);
};
export const isAsyncFunction = (f) => {
    if (typeof f !== 'function') {
        return false;
    }
    const s = f.toString().trim();
    // not a great way to determine but there does not appear to be any
    // other way by conventional means
    return !!(s.match(/^async /) || s.match(/return __awaiter/));
};
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const isFunction = (x) => {
    if (isDate(x)) {
        return false;
    }
    return typeof x === 'function';
};
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const isObject = (x) => x !== null && Object(x) === x;
//# sourceMappingURL=functions.js.map