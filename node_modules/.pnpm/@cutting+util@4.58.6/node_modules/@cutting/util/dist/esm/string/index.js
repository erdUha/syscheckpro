export const decamelize = (str) => str.replace(/([a-z\d])([A-Z])/g, '$1_$2').toLowerCase();
export const dasherize = (str) => decamelize(str).replace(/[ _]/g, '-');
export const stripSpaces = (txt) => (txt || '').replace(/\s/g, '');
export const capitalize = (str) => str?.replace(/^[a-z]/, (chr) => chr.toUpperCase());
export const padNumber = (num) => {
    const s = String(num || '');
    return s.length !== 1 ? s : `0${s}`;
};
//# sourceMappingURL=index.js.map